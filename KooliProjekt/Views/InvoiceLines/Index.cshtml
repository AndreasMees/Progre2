@model KooliProjekt.Models.InvoiceLinesIndexModel
@{
    ViewData["Title"] = "Invoice Lines";
}
<h1>Invoice Lines</h1>
<p>
    <a asp-action="Create" class="btn btn-primary">Create New Invoice Line</a>
</p>
<form method="get">
    <div class="form-group">
        <label class="control-label">Keyword</label>
        <input asp-for="Search.Keyword" class="form-control" />
    </div>
    <div class="form-group mt-2">
        <button class="btn btn-primary">Search</button>
        <a asp-action="Index" class="btn btn-secondary">Reset</a>
    </div>
</form>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Line Item</th>
            <th>Unit Price</th>
            <th>Quantity</th>
            <th>VAT Rate</th>
            <th>Total</th>
            <th>Invoice</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Data.Results)
        {
            <tr>
                <td>@item.LineItem</td>
                <td>@item.UnitPrice</td>
                <td>@item.Quantity</td>
                <td>@item.VatRate.ToString("P")</td>
                <td>@item.Total</td>
                <td>@item.Invoice?.InvoiceNo</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@(await Component.InvokeAsync<KooliProjekt.Components.PagerViewComponent>(Model.Data))